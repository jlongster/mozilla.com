Setup
-----

1) cp settings.example settings.py
   
   - edit this file.  changes values appropriately

2) pip install -r requirements.txt

Run Tests
---------

nosetests testFile.py

Or, run them all with,

find . -name 'test*.py' | grep -v venv | xargs nosetests
